diff --git a/node_modules/react-native-maps/android/build.gradle b/node_modules/react-native-maps/android/build.gradle
index 0070417..737b5b4 100644
--- a/node_modules/react-native-maps/android/build.gradle
+++ b/node_modules/react-native-maps/android/build.gradle
@@ -98,7 +98,7 @@ repositories {
 dependencies {
     implementation 'com.facebook.react:react-native:+'
     implementation "com.google.android.gms:play-services-base:${safeExtGet('googlePlayServicesBaseVersion', '18.5.0')}"
-    implementation "com.google.android.gms:play-services-maps:${safeExtGet('googlePlayServicesMapsVersion', '19.1.0')}"
+    implementation "com.google.android.gms:play-services-maps:${safeExtGet('googlePlayServicesMapsVersion', '20.0.0')}"
     implementation "com.google.android.gms:play-services-location:${safeExtGet('googlePlayServicesLocationVersion', '21.3.0')}"
     implementation 'com.google.maps.android:android-maps-utils:3.10.0'
     implementation "androidx.work:work-runtime:2.9.1"
diff --git a/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMapViewManagerDelegate.java b/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMapViewManagerDelegate.java
index 37d54fc..795aa72 100644
--- a/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMapViewManagerDelegate.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMapViewManagerDelegate.java
@@ -175,6 +175,9 @@ public class RNMapsMapViewManagerDelegate<T extends View, U extends BaseViewMana
       case "showsTraffic":
         mViewManager.setShowsTraffic(view, value == null ? false : (boolean) value);
         break;
+      case "showsTransit":
+        mViewManager.setShowsTransit(view, value == null ? false : (boolean) value);
+        break;
       case "zoomTapEnabled":
         mViewManager.setZoomTapEnabled(view, value == null ? true : (boolean) value);
         break;
diff --git a/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMapViewManagerInterface.java b/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMapViewManagerInterface.java
index 47771dc..af491a2 100644
--- a/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMapViewManagerInterface.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/facebook/react/viewmanagers/RNMapsMapViewManagerInterface.java
@@ -66,6 +66,7 @@ public interface RNMapsMapViewManagerInterface<T extends View> extends ViewManag
   void setZoomControlEnabled(T view, boolean value);
   void setZoomEnabled(T view, boolean value);
   void setShowsTraffic(T view, boolean value);
+  void setShowsTransit(T view, boolean value);
   void setZoomTapEnabled(T view, boolean value);
   void setCameraZoomRange(T view, @Nullable ReadableMap value);
   void animateToRegion(T view, String regionJSON, int duration);
diff --git a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/fabric/MapViewManager.java b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/fabric/MapViewManager.java
index 555ab38..1199d55 100644
--- a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/fabric/MapViewManager.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/fabric/MapViewManager.java
@@ -524,6 +524,12 @@ public class MapViewManager extends ViewGroupManager<MapView> implements RNMapsM
         view.setShowsTraffic(value);
     }
 
+    /** Google Maps transit network layer (Hither patch; New Architecture path). */
+    @Override
+    public void setShowsTransit(MapView view, boolean value) {
+        view.setShowsTransit(value);
+    }
+
     @Override
     public void setZoomTapEnabled(MapView view, boolean value) {
         // not supported
diff --git a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapManager.java b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapManager.java
index fb78287..ac1cdcd 100644
--- a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapManager.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapManager.java
@@ -238,6 +238,12 @@ public class MapManager extends ViewGroupManager<MapView> {
         view.map.setTrafficEnabled(showTraffic);
     }
 
+    /** Google Maps transit network layer (SDK 20+). No-op when map not ready. */
+    @ReactProp(name = "showsTransit", defaultBoolean = false)
+    public void setShowsTransit(MapView view, boolean showTransit) {
+        view.setShowsTransit(showTransit);
+    }
+
     @ReactProp(name = "showsBuildings", defaultBoolean = false)
     public void setShowBuildings(MapView view, boolean showBuildings) {
         view.setShowBuildings(showBuildings);
diff --git a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapView.java b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapView.java
index ebf8780..5cc473a 100644
--- a/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapView.java
+++ b/node_modules/react-native-maps/android/src/main/java/com/rnmaps/maps/MapView.java
@@ -120,6 +120,7 @@ public class MapView extends com.google.android.gms.maps.MapView implements Goog
     private boolean showMyLocationButton = true;
 
     private boolean showsTraffic = false;
+    private boolean showsTransit = false;
 
     private boolean handlePanDrag = false;
     private boolean moveOnMarkerPress = true;
@@ -441,6 +442,17 @@ public class MapView extends com.google.android.gms.maps.MapView implements Goog
         }
     }
 
+    public void setShowsTransit(boolean value) {
+        showsTransit = value;
+        if (map != null) {
+            try {
+                map.setTransitEnabled(value);
+            } catch (Throwable ignored) {
+                // Older Play Services Maps without transit layer — keep base map.
+            }
+        }
+    }
+
 
     @FunctionalInterface
     public interface EventCreator<T extends Event> {
@@ -695,7 +707,11 @@ public class MapView extends com.google.android.gms.maps.MapView implements Goog
 
 
         map.setTrafficEnabled(showsTraffic);
-
+        try {
+            map.setTransitEnabled(showsTransit);
+        } catch (Throwable ignored) {
+            // Older Play Services Maps without transit layer — keep base map.
+        }
 
         isMapReady = true;
         if (kmlSrc != null) {
diff --git a/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.cpp b/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.cpp
index e157ffd..16004ee 100644
--- a/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.cpp
+++ b/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.cpp
@@ -131,6 +131,7 @@ RNMapsGoogleMapViewProps::RNMapsGoogleMapViewProps(
     showsMyLocationButton(convertRawProp(context, rawProps, "showsMyLocationButton", sourceProps.showsMyLocationButton, {false})),
     showsScale(convertRawProp(context, rawProps, "showsScale", sourceProps.showsScale, {false})),
     showsTraffic(convertRawProp(context, rawProps, "showsTraffic", sourceProps.showsTraffic, {false})),
+    showsTransit(convertRawProp(context, rawProps, "showsTransit", sourceProps.showsTransit, {false})),
     showsUserLocation(convertRawProp(context, rawProps, "showsUserLocation", sourceProps.showsUserLocation, {false})),
     userInterfaceStyle(convertRawProp(context, rawProps, "userInterfaceStyle", sourceProps.userInterfaceStyle, {RNMapsGoogleMapViewUserInterfaceStyle::System})),
     customMapStyleString(convertRawProp(context, rawProps, "customMapStyleString", sourceProps.customMapStyleString, {})),
@@ -250,6 +251,10 @@ folly::dynamic RNMapsGoogleMapViewProps::getDiffProps(
     result["showsTraffic"] = showsTraffic;
   }
 
+  if (showsTransit != oldProps->showsTransit) {
+    result["showsTransit"] = showsTransit;
+  }
+
   if (showsUserLocation != oldProps->showsUserLocation) {
     result["showsUserLocation"] = showsUserLocation;
   }
@@ -406,6 +411,7 @@ RNMapsMapViewProps::RNMapsMapViewProps(
     zoomControlEnabled(convertRawProp(context, rawProps, "zoomControlEnabled", sourceProps.zoomControlEnabled, {true})),
     zoomEnabled(convertRawProp(context, rawProps, "zoomEnabled", sourceProps.zoomEnabled, {true})),
     showsTraffic(convertRawProp(context, rawProps, "showsTraffic", sourceProps.showsTraffic, {false})),
+    showsTransit(convertRawProp(context, rawProps, "showsTransit", sourceProps.showsTransit, {false})),
     zoomTapEnabled(convertRawProp(context, rawProps, "zoomTapEnabled", sourceProps.zoomTapEnabled, {true})),
     cameraZoomRange(convertRawProp(context, rawProps, "cameraZoomRange", sourceProps.cameraZoomRange, {})) {}
 
@@ -625,6 +631,10 @@ folly::dynamic RNMapsMapViewProps::getDiffProps(
     result["showsTraffic"] = showsTraffic;
   }
 
+  if (showsTransit != oldProps->showsTransit) {
+    result["showsTransit"] = showsTransit;
+  }
+
   if (zoomTapEnabled != oldProps->zoomTapEnabled) {
     result["zoomTapEnabled"] = zoomTapEnabled;
   }
diff --git a/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.h b/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.h
index f7b7757..f44d7db 100644
--- a/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.h
+++ b/node_modules/react-native-maps/android/src/main/jni/react/renderer/components/RNMapsSpecs/Props.h
@@ -572,6 +572,7 @@ class RNMapsGoogleMapViewProps final : public ViewProps {
   bool showsMyLocationButton{false};
   bool showsScale{false};
   bool showsTraffic{false};
+  bool showsTransit{false};
   bool showsUserLocation{false};
   RNMapsGoogleMapViewUserInterfaceStyle userInterfaceStyle{RNMapsGoogleMapViewUserInterfaceStyle::System};
   std::string customMapStyleString{};
@@ -1374,6 +1375,7 @@ class RNMapsMapViewProps final : public ViewProps {
   bool zoomControlEnabled{true};
   bool zoomEnabled{true};
   bool showsTraffic{false};
+  bool showsTransit{false};
   bool zoomTapEnabled{true};
   RNMapsMapViewCameraZoomRangeStruct cameraZoomRange{};
 
diff --git a/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMap.h b/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMap.h
index 8f21a3a..2b70eb3 100644
--- a/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMap.h
+++ b/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMap.h
@@ -54,6 +54,8 @@
 
 @property (nonatomic, assign) BOOL showsBuildings;
 @property (nonatomic, assign) BOOL showsTraffic;
+/** Google Maps transit network layer. */
+@property (nonatomic, assign) BOOL showsTransit;
 @property (nonatomic, assign) BOOL showsCompass;
 @property (nonatomic, assign) BOOL scrollEnabled;
 @property (nonatomic, assign) BOOL zoomEnabled;
diff --git a/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMap.mm b/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMap.mm
index b375241..d510d58 100644
--- a/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMap.mm
+++ b/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMap.mm
@@ -656,6 +656,14 @@ id regionAsJSON(MKCoordinateRegion region) {
   return self.trafficEnabled;
 }
 
+- (void)setShowsTransit:(BOOL)showsTransit {
+  self.transitEnabled = showsTransit;
+}
+
+- (BOOL)showsTransit {
+  return self.transitEnabled;
+}
+
 - (void)setShowsBuildings:(BOOL)showsBuildings {
   self.buildingsEnabled = showsBuildings;
 }
diff --git a/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMapManager.mm b/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMapManager.mm
index 2dd2f5a..aa8cf78 100644
--- a/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMapManager.mm
+++ b/node_modules/react-native-maps/ios/AirGoogleMaps/AIRGoogleMapManager.mm
@@ -93,6 +93,7 @@ RCT_EXPORT_VIEW_PROPERTY(showsBuildings, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(showsCompass, BOOL)
 //RCT_EXPORT_VIEW_PROPERTY(showsScale, BOOL)  // Not supported by GoogleMaps
 RCT_EXPORT_VIEW_PROPERTY(showsTraffic, BOOL)
+RCT_EXPORT_VIEW_PROPERTY(showsTransit, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(zoomEnabled, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(rotateEnabled, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(scrollEnabled, BOOL)
diff --git a/node_modules/react-native-maps/ios/AirMaps/AIRMap.h b/node_modules/react-native-maps/ios/AirMaps/AIRMap.h
index 17c1755..a4761d6 100644
--- a/node_modules/react-native-maps/ios/AirMaps/AIRMap.h
+++ b/node_modules/react-native-maps/ios/AirMaps/AIRMap.h
@@ -51,6 +51,8 @@ extern const NSInteger AIRMapMaxZoomLevel;
 @property (nonatomic, assign) CGPoint compassOffset;
 @property (nonatomic, assign) UIEdgeInsets mapPadding;
 @property (nonatomic, assign) BOOL showsPointsOfInterests;
+/** Native MapKit heading cone without forcing userTrackingMode followWithHeading. */
+@property (nonatomic, assign) BOOL showsUserHeadingIndicator;
 @property (nonatomic, copy) NSArray<NSString *> *pointsOfInterestFilter;
 
 @property (nonatomic, assign) CLLocationCoordinate2D pendingCenter;
@@ -82,6 +84,9 @@ extern const NSInteger AIRMapMaxZoomLevel;
 - (void)finishLoading;
 - (double)getZoomLevel;
 - (NSDictionary *)getMapBoundaries;
+- (void)updateHeadingIndicator;
+- (BOOL)isHitherHeadingAnnotation:(id<MKAnnotation>)annotation;
+- (MKAnnotationView *)hitherHeadingAnnotationView;
 
 - (AIRMapMarker*) markerAtPoint:(CGPoint)point;
 - (NSDictionary*) getMarkersFramesWithOnlyVisible:(BOOL)onlyVisible;
diff --git a/node_modules/react-native-maps/ios/AirMaps/AIRMap.mm b/node_modules/react-native-maps/ios/AirMaps/AIRMap.mm
index f7da1e0..123fa1b 100644
--- a/node_modules/react-native-maps/ios/AirMaps/AIRMap.mm
+++ b/node_modules/react-native-maps/ios/AirMaps/AIRMap.mm
@@ -11,6 +11,7 @@
 
 #import <React/RCTEventDispatcher.h>
 #import <React/UIView+React.h>
+#import <CoreLocation/CoreLocation.h>
 #import "AIRMapMarker.h"
 #import "AIRMapPolyline.h"
 #import "AIRMapPolygon.h"
@@ -38,12 +39,20 @@ const NSInteger AIRMapMaxZoomLevel = 20;
 
 @end
 
-@interface AIRMap ()
+@interface AIRMap () <CLLocationManagerDelegate>
 
 @property (nonatomic, strong) UIActivityIndicatorView *activityIndicatorView;
 @property (nonatomic, assign) NSNumber *shouldZoomEnabled;
 @property (nonatomic, assign) NSNumber *shouldScrollEnabled;
-
+@property (nonatomic, strong) CLLocationManager *hitherHeadingManager;
+@property (nonatomic, strong) MKPointAnnotation *hitherHeadingAnnotation;
+@property (nonatomic, strong) MKAnnotationView *hitherHeadingView;
+@property (nonatomic, assign) CLLocationDirection hitherHeading;
+
+- (void)startHeadingIndicatorIfNeeded;
+- (void)stopHeadingIndicator;
+- (void)hitherDidEnterBackground;
+- (void)hitherWillEnterForeground;
 - (void)updateScrollEnabled;
 - (void)updateZoomEnabled;
 
@@ -60,6 +69,7 @@ const NSInteger AIRMapMaxZoomLevel = 20;
     BOOL _initialized;
     BOOL _loadingStarted;
     BOOL _showsPointsOfInterests;
+    BOOL _showsUserHeadingIndicator;
     NSArray<NSString *> *_pointsOfInterestFilter;
 
     // Array to manually track RN subviews
@@ -110,11 +120,125 @@ const NSInteger AIRMapMaxZoomLevel = 20;
         _initialized = YES;
         _showsPointsOfInterests = YES;
         _pointsOfInterestFilter = @[];
+        _hitherHeading = -1;
         [self listenToMemoryWarnings];
+        [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(hitherDidEnterBackground) name:UIApplicationDidEnterBackgroundNotification object:nil];
+        [[NSNotificationCenter defaultCenter] addObserver:self selector:@selector(hitherWillEnterForeground) name:UIApplicationWillEnterForegroundNotification object:nil];
     }
     return self;
 }
 
+- (void)dealloc
+{
+    [self stopHeadingIndicator];
+    [[NSNotificationCenter defaultCenter] removeObserver:self];
+}
+
+- (void)setShowsUserHeadingIndicator:(BOOL)showsUserHeadingIndicator
+{
+    _showsUserHeadingIndicator = showsUserHeadingIndicator;
+    if (showsUserHeadingIndicator) [self startHeadingIndicatorIfNeeded];
+    else [self stopHeadingIndicator];
+}
+
+- (void)setShowsUserLocation:(BOOL)showsUserLocation
+{
+    [super setShowsUserLocation:showsUserLocation];
+    if (showsUserLocation) [self startHeadingIndicatorIfNeeded];
+    else [self stopHeadingIndicator];
+}
+
+- (void)startHeadingIndicatorIfNeeded
+{
+    if (!self.showsUserHeadingIndicator || !self.showsUserLocation
+        || UIApplication.sharedApplication.applicationState != UIApplicationStateActive
+        || ![CLLocationManager headingAvailable]) return;
+    if (!self.hitherHeadingManager) {
+        self.hitherHeadingManager = [CLLocationManager new];
+        self.hitherHeadingManager.delegate = self;
+        self.hitherHeadingManager.headingFilter = 5.0;
+        self.hitherHeadingManager.headingOrientation = CLDeviceOrientationPortrait;
+    }
+    [self.hitherHeadingManager startUpdatingHeading];
+    [self updateHeadingIndicator];
+}
+
+- (void)stopHeadingIndicator
+{
+    [self.hitherHeadingManager stopUpdatingHeading];
+    self.hitherHeadingManager.delegate = nil;
+    self.hitherHeadingManager = nil;
+    self.hitherHeading = -1;
+    if (self.hitherHeadingAnnotation) [self removeAnnotation:self.hitherHeadingAnnotation];
+    self.hitherHeadingAnnotation = nil;
+    self.hitherHeadingView = nil;
+}
+
+- (void)hitherDidEnterBackground { [self stopHeadingIndicator]; }
+- (void)hitherWillEnterForeground { [self startHeadingIndicatorIfNeeded]; }
+
+- (void)locationManager:(CLLocationManager *)manager didUpdateHeading:(CLHeading *)newHeading
+{
+    if (manager != self.hitherHeadingManager) return;
+    CLLocationDirection heading = newHeading.trueHeading >= 0
+        ? newHeading.trueHeading
+        : newHeading.magneticHeading;
+    CLLocation *location = self.userLocation.location;
+    if (heading < 0 || location == nil || !CLLocationCoordinate2DIsValid(location.coordinate)) return;
+    self.hitherHeading = heading;
+    [self updateHeadingIndicator];
+}
+
+- (BOOL)isHitherHeadingAnnotation:(id<MKAnnotation>)annotation
+{
+    return annotation != nil && annotation == self.hitherHeadingAnnotation;
+}
+
+- (MKAnnotationView *)hitherHeadingAnnotationView
+{
+    if (self.hitherHeadingView) return self.hitherHeadingView;
+    if (!self.hitherHeadingAnnotation) return nil;
+
+    MKAnnotationView *view = [[MKAnnotationView alloc] initWithAnnotation:self.hitherHeadingAnnotation reuseIdentifier:@"hither.heading"];
+    view.enabled = NO;
+    view.canShowCallout = NO;
+    view.displayPriority = MKFeatureDisplayPriorityDefaultLow;
+    view.layer.zPosition = -1;
+    view.bounds = CGRectMake(0, 0, 112, 112);
+    view.centerOffset = CGPointZero;
+
+    CAShapeLayer *cone = [CAShapeLayer layer];
+    UIBezierPath *path = [UIBezierPath bezierPath];
+    CGPoint center = CGPointMake(56, 56);
+    [path moveToPoint:CGPointMake(center.x, center.y - 12)];
+    [path addLineToPoint:CGPointMake(22, 10)];
+    [path addQuadCurveToPoint:CGPointMake(90, 10) controlPoint:CGPointMake(56, -2)];
+    [path closePath];
+    cone.path = path.CGPath;
+    cone.fillColor = [UIColor colorWithRed:0.18 green:0.55 blue:1.0 alpha:0.30].CGColor;
+    cone.strokeColor = [UIColor colorWithRed:0.18 green:0.55 blue:1.0 alpha:0.18].CGColor;
+    cone.lineWidth = 1.0;
+    [view.layer addSublayer:cone];
+    self.hitherHeadingView = view;
+    return view;
+}
+
+- (void)updateHeadingIndicator
+{
+    if (!self.showsUserHeadingIndicator || !self.showsUserLocation || self.hitherHeading < 0) return;
+    CLLocation *location = self.userLocation.location;
+    if (location == nil) return;
+    CLLocationCoordinate2D coordinate = location.coordinate;
+    if (!CLLocationCoordinate2DIsValid(coordinate)) return;
+    if (!self.hitherHeadingAnnotation) {
+        self.hitherHeadingAnnotation = [MKPointAnnotation new];
+        [self addAnnotation:self.hitherHeadingAnnotation];
+    }
+    self.hitherHeadingAnnotation.coordinate = coordinate;
+    MKAnnotationView *view = [self hitherHeadingAnnotationView];
+    if (view) view.transform = CGAffineTransformMakeRotation((self.hitherHeading - self.camera.heading) * M_PI / 180.0);
+}
+
 -(void)addSubview:(UIView *)view {
     if([view isKindOfClass:[AIRMapMarker class]]) {
         [self addAnnotation:(id <MKAnnotation>)view];
@@ -521,16 +645,6 @@ const NSInteger AIRMapMaxZoomLevel = 20;
         UIGraphicsEndImageContext();
     }];
 }
-
-
-
-- (void)setShowsUserLocation:(BOOL)showsUserLocation
-{
-    if (self.showsUserLocation != showsUserLocation) {
-        super.showsUserLocation = showsUserLocation;
-    }
-}
-
 - (void)setUserInterfaceStyle:(NSString*)userInterfaceStyle
 {
         if([userInterfaceStyle isEqualToString:@"light"]) {
diff --git a/node_modules/react-native-maps/ios/AirMaps/AIRMapManager.m b/node_modules/react-native-maps/ios/AirMaps/AIRMapManager.m
index d034034..86e0745 100644
--- a/node_modules/react-native-maps/ios/AirMaps/AIRMapManager.m
+++ b/node_modules/react-native-maps/ios/AirMaps/AIRMapManager.m
@@ -84,6 +84,7 @@ RCT_EXPORT_MODULE()
 RCT_EXPORT_VIEW_PROPERTY(isAccessibilityElement, BOOL)
 RCT_REMAP_VIEW_PROPERTY(testID, accessibilityIdentifier, NSString)
 RCT_EXPORT_VIEW_PROPERTY(showsUserLocation, BOOL)
+RCT_EXPORT_VIEW_PROPERTY(showsUserHeadingIndicator, BOOL)
 RCT_EXPORT_VIEW_PROPERTY(tintColor, UIColor)
 RCT_EXPORT_VIEW_PROPERTY(userLocationAnnotationTitle, NSString)
 RCT_EXPORT_VIEW_PROPERTY(userInterfaceStyle, NSString)
@@ -797,6 +798,9 @@ RCT_EXPORT_METHOD(getAddressFromCoordinates:(nonnull NSNumber *)reactTag
 
 - (MKAnnotationView *)mapView:(__unused AIRMap *)mapView viewForAnnotation:(AIRMapMarker *)marker
 {
+    if ([mapView isHitherHeadingAnnotation:marker]) {
+        return [mapView hitherHeadingAnnotationView];
+    }
     if (![marker isKindOfClass:[AIRMapMarker class]]) {
         if ([marker isKindOfClass:[MKUserLocation class]] && mapView.userLocationAnnotationTitle != nil) {
             [(MKUserLocation*)marker setTitle: mapView.userLocationAnnotationTitle];
@@ -890,6 +894,7 @@ static int kDragCenterContext;
 
 - (void)mapView:(AIRMap *)mapView didUpdateUserLocation:(MKUserLocation *)location
 {
+    [mapView updateHeadingIndicator];
     id event = @{@"coordinate": @{
                          @"latitude": @(location.coordinate.latitude),
                          @"longitude": @(location.coordinate.longitude),
@@ -931,11 +936,13 @@ static int kDragCenterContext;
 
 - (void)mapViewDidChangeVisibleRegion:(AIRMap *)mapView
 {
+    [mapView updateHeadingIndicator];
     [self _regionChanged:mapView continuous:YES];
 }
 
 - (void)mapView:(AIRMap *)mapView regionDidChangeAnimated:(__unused BOOL)animated
 {
+    [mapView updateHeadingIndicator];
     // Don't send region did change events until map has
     // started rendering, as these won't represent the final location
     if (animated && mapView.ignoreRegionChanges){
diff --git a/node_modules/react-native-maps/ios/AirMaps/RNMapsMapView.mm b/node_modules/react-native-maps/ios/AirMaps/RNMapsMapView.mm
index 447520d..91065a9 100644
--- a/node_modules/react-native-maps/ios/AirMaps/RNMapsMapView.mm
+++ b/node_modules/react-native-maps/ios/AirMaps/RNMapsMapView.mm
@@ -564,6 +564,7 @@ newViewProps.name.right);           \
     REMAP_MAPVIEW_PROP(showsCompass)
     REMAP_MAPVIEW_PROP(showsScale)
     REMAP_MAPVIEW_PROP(showsUserLocation)
+    REMAP_MAPVIEW_PROP(showsUserHeadingIndicator)
     REMAP_MAPVIEW_PROP(userLocationCalloutEnabled)
     REMAP_MAPVIEW_PROP(zoomEnabled)
     REMAP_MAPVIEW_PROP(loadingEnabled)
diff --git a/node_modules/react-native-maps/ios/generated/RNMapsSpecs/Props.cpp b/node_modules/react-native-maps/ios/generated/RNMapsSpecs/Props.cpp
index ba41078..9a64a88 100644
--- a/node_modules/react-native-maps/ios/generated/RNMapsSpecs/Props.cpp
+++ b/node_modules/react-native-maps/ios/generated/RNMapsSpecs/Props.cpp
@@ -132,6 +132,7 @@ RNMapsGoogleMapViewProps::RNMapsGoogleMapViewProps(
     showsScale(convertRawProp(context, rawProps, "showsScale", sourceProps.showsScale, {false})),
     showsTraffic(convertRawProp(context, rawProps, "showsTraffic", sourceProps.showsTraffic, {false})),
     showsUserLocation(convertRawProp(context, rawProps, "showsUserLocation", sourceProps.showsUserLocation, {false})),
+    showsUserHeadingIndicator(convertRawProp(context, rawProps, "showsUserHeadingIndicator", sourceProps.showsUserHeadingIndicator, {false})),
     userInterfaceStyle(convertRawProp(context, rawProps, "userInterfaceStyle", sourceProps.userInterfaceStyle, {RNMapsGoogleMapViewUserInterfaceStyle::System})),
     customMapStyleString(convertRawProp(context, rawProps, "customMapStyleString", sourceProps.customMapStyleString, {})),
     userLocationCalloutEnabled(convertRawProp(context, rawProps, "userLocationCalloutEnabled", sourceProps.userLocationCalloutEnabled, {false})),
@@ -253,6 +254,9 @@ folly::dynamic RNMapsGoogleMapViewProps::getDiffProps(
   if (showsUserLocation != oldProps->showsUserLocation) {
     result["showsUserLocation"] = showsUserLocation;
   }
+  if (showsUserHeadingIndicator != oldProps->showsUserHeadingIndicator) {
+    result["showsUserHeadingIndicator"] = showsUserHeadingIndicator;
+  }
 
   if (userInterfaceStyle != oldProps->userInterfaceStyle) {
     result["userInterfaceStyle"] = toDynamic(userInterfaceStyle);
@@ -395,6 +399,7 @@ RNMapsMapViewProps::RNMapsMapViewProps(
     showsMyLocationButton(convertRawProp(context, rawProps, "showsMyLocationButton", sourceProps.showsMyLocationButton, {false})),
     showsScale(convertRawProp(context, rawProps, "showsScale", sourceProps.showsScale, {false})),
     showsUserLocation(convertRawProp(context, rawProps, "showsUserLocation", sourceProps.showsUserLocation, {false})),
+    showsUserHeadingIndicator(convertRawProp(context, rawProps, "showsUserHeadingIndicator", sourceProps.showsUserHeadingIndicator, {false})),
     tintColor(convertRawProp(context, rawProps, "tintColor", sourceProps.tintColor, {})),
     toolbarEnabled(convertRawProp(context, rawProps, "toolbarEnabled", sourceProps.toolbarEnabled, {true})),
     userInterfaceStyle(convertRawProp(context, rawProps, "userInterfaceStyle", sourceProps.userInterfaceStyle, {RNMapsMapViewUserInterfaceStyle::System})),
@@ -581,6 +586,9 @@ folly::dynamic RNMapsMapViewProps::getDiffProps(
   if (showsUserLocation != oldProps->showsUserLocation) {
     result["showsUserLocation"] = showsUserLocation;
   }
+  if (showsUserHeadingIndicator != oldProps->showsUserHeadingIndicator) {
+    result["showsUserHeadingIndicator"] = showsUserHeadingIndicator;
+  }
 
   if (tintColor != oldProps->tintColor) {
     result["tintColor"] = *tintColor;
diff --git a/node_modules/react-native-maps/ios/generated/RNMapsSpecs/Props.h b/node_modules/react-native-maps/ios/generated/RNMapsSpecs/Props.h
index 1249045..230757c 100644
--- a/node_modules/react-native-maps/ios/generated/RNMapsSpecs/Props.h
+++ b/node_modules/react-native-maps/ios/generated/RNMapsSpecs/Props.h
@@ -573,6 +573,7 @@ class RNMapsGoogleMapViewProps final : public ViewProps {
   bool showsScale{false};
   bool showsTraffic{false};
   bool showsUserLocation{false};
+  bool showsUserHeadingIndicator{false};
   RNMapsGoogleMapViewUserInterfaceStyle userInterfaceStyle{RNMapsGoogleMapViewUserInterfaceStyle::System};
   std::string customMapStyleString{};
   bool userLocationCalloutEnabled{false};
@@ -1414,6 +1415,7 @@ class RNMapsMapViewProps final : public ViewProps {
   bool showsMyLocationButton{false};
   bool showsScale{false};
   bool showsUserLocation{false};
+  bool showsUserHeadingIndicator{false};
   SharedColor tintColor{};
   bool toolbarEnabled{true};
   RNMapsMapViewUserInterfaceStyle userInterfaceStyle{RNMapsMapViewUserInterfaceStyle::System};
diff --git a/node_modules/react-native-maps/src/MapView.tsx b/node_modules/react-native-maps/src/MapView.tsx
index 6fd5565..b9da02b 100644
--- a/node_modules/react-native-maps/src/MapView.tsx
+++ b/node_modules/react-native-maps/src/MapView.tsx
@@ -638,6 +638,16 @@ export type MapViewProps = ViewProps & {
    */
   showsTraffic?: boolean;
 
+  /**
+   * Google Maps transit network layer (stations + lines). No-op when the
+   * installed Maps SDK lacks the API or the city has no transit coverage.
+   *
+   * @default false
+   * @platform iOS: Google Maps only
+   * @platform Android: Google Maps
+   */
+  showsTransit?: boolean;
+
   /**
    * If `true` the users location will be displayed on the map.
    *
@@ -649,6 +659,9 @@ export type MapViewProps = ViewProps & {
    */
   showsUserLocation?: boolean;
 
+  /** iOS MapKit heading cone without forcing followWithHeading. */
+  showsUserHeadingIndicator?: boolean;
+
   /**
    * Sets the tint color of the map. (Changes the color of the position indicator)
    *
diff --git a/node_modules/react-native-maps/src/specs/NativeComponentGoogleMapView.ts b/node_modules/react-native-maps/src/specs/NativeComponentGoogleMapView.ts
index d67c272..baaf1c3 100644
--- a/node_modules/react-native-maps/src/specs/NativeComponentGoogleMapView.ts
+++ b/node_modules/react-native-maps/src/specs/NativeComponentGoogleMapView.ts
@@ -745,6 +745,14 @@ export interface MapFabricNativeProps extends ViewProps {
    */
   showsTraffic?: boolean;
 
+  /**
+   * Google Maps transit network layer (stations + lines).
+   * @default false
+   * @platform iOS: Google Maps only
+   * @platform Android: Supported
+   */
+  showsTransit?: boolean;
+
   /**
    * If `true` the users location will be displayed on the map.
    *
diff --git a/node_modules/react-native-maps/src/specs/NativeComponentMapView.ts b/node_modules/react-native-maps/src/specs/NativeComponentMapView.ts
index b4fd5c1..3a1abe7 100644
--- a/node_modules/react-native-maps/src/specs/NativeComponentMapView.ts
+++ b/node_modules/react-native-maps/src/specs/NativeComponentMapView.ts
@@ -926,6 +926,9 @@ export interface MapFabricNativeProps extends ViewProps {
    */
   showsUserLocation?: boolean;
 
+  /** iOS MapKit heading cone; does not force followWithHeading. */
+  showsUserHeadingIndicator?: boolean;
+
   /**
    * Sets the tint color of the map. (Changes the color of the position indicator)
    *
@@ -1047,6 +1050,16 @@ export interface MapFabricNativeProps extends ViewProps {
    */
   showsTraffic?: boolean;
 
+  /**
+   * Google Maps transit network layer (stations + lines).
+   * No-op on Apple MapKit provider.
+   *
+   * @default false
+   * @platform iOS: Google Maps only
+   * @platform Android: Supported
+   */
+  showsTransit?: boolean;
+
   /**
    * If `false` the user won't be able to double tap to zoom the map.
    * **Note:** But it will greatly decrease delay of tap gesture recognition.
 