# Supabase 連線設定（Expo 在 build 時把 EXPO_PUBLIC_* 注入 client bundle）
# 這裡只放可公開的 publishable key；service_role 絕不可放進 client。
EXPO_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_xxx
