#!/usr/bin/env sh
# Compatibility entrypoint for old hooks: intentionally performs no mutation.
printf '%s\n' 'Automatic task/commit shipping is retired. Use an explicitly scoped release workflow.'
exit 0
