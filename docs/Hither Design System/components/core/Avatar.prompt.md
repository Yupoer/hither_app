Circular member avatar with a colored role ring. Holds an emoji, initials, or image. `leader` adds a star badge; `dimmed` shows offline/lost.

```jsx
<Avatar emoji="🐑" color="pink" size={46} />
<Avatar emoji="⚽️" color="success" leader ring />
```
