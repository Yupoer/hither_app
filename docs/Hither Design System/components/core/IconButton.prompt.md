Circular icon button — the round controls that float on the map (+, bell, locate). Pass an icon glyph/SVG as children.

```jsx
<IconButton tone="glass" size={44}><i className="ph ph-plus" /></IconButton>
```

Tones: `neutral`, `signal`, `sky`, `glass` (blurred translucent). Minimum size 44px for tap targets.
