A map teardrop pin holding an emoji/initial, ringed white so it pops on the basemap. `pulse` adds a live-locating halo; `gather` is the destination beacon (flag).

```jsx
<MemberMarker emoji="🦊" color="sky" pulse />
<MemberMarker gather color="signal" />
```
