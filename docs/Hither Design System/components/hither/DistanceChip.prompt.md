Twin readout of walking time + straight-line distance to a gather point — Hither's core "how far, how long" answer.

```jsx
<DistanceChip time="4 min" distance="323 m" />
<DistanceChip time="即將抵達" distance="40 m" size="lg" tone="accent" />
```
