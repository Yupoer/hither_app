List row with a rounded icon tile, a title, and a trailing action/chevron — used for gather points, KML import, and settings entries.

```jsx
<GatherPointRow icon="🚩" title="0 個集合點 · 調整順序" trailing={<span style={{color:'var(--accent)'}}>編輯</span>} />
```
