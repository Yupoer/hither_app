Segmented control with a sliding accent pill — the Leader / Follower role toggle and view switchers.

```jsx
<Segmented options={[{label:'隊長',value:'leader'},{label:'隊員',value:'follower'}]} value={role} onChange={setRole} />
```
